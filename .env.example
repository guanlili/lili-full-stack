# 复制为 .env 并填写实际值（新项目直接用 scripts/init-project.sh 生成）
# cp .env.example .env
#
# ⚠️ 新增/删除配置项时，必须同步 .github/workflows/deploy.yml 里「写入 .env」的键列表，
#    否则该变量在生产上是空值。CI 会自动校验两边一致性，不一致直接失败。

# 前端地址（开发时是 localhost:5173，生产时改为域名）
FRONTEND_HOST=http://localhost:5173

# 运行环境: local | staging | production
ENVIRONMENT=local

# 项目名称（显示在邮件等处）
PROJECT_NAME="My Project"

# 允许跨域的前端地址
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,http://localhost:80"

# 随机字符串，用于 JWT 签名（生产环境必须修改）
SECRET_KEY=changethis

# 初始管理员账号
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis

# 邮件配置（可选，不填则禁用邮件功能）
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# 数据库
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changethis

# Sentry 错误监控（可选）
SENTRY_DSN=

# Docker 端口配置
APP_PORT=80

# 后端 worker 进程数（默认 1，生产大流量可调至 cpu核数×2+1）
WORKERS=1
